.deploy_remote_template:
  stage: deploy
  image: alpine:3.21
  script:
  - test -n "${DEPLOY_TARGET_HOST:-}" || { echo "DEPLOY_TARGET_HOST is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_USER:-}" || { echo "DEPLOY_TARGET_USER is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_APP_DIR:-}" || { echo "DEPLOY_TARGET_APP_DIR is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_SSH_KEY:-}" || { echo "DEPLOY_TARGET_SSH_KEY is required"; exit 1; }
  - test -n "${BACKEND_IMAGE_REF:-}" || { echo "BACKEND_IMAGE_REF is required"; exit 1; }
  - test -n "${FRONTEND_IMAGE_REF:-}" || { echo "FRONTEND_IMAGE_REF is required"; exit 1; }
  - test -n "${ADMIN_IMAGE_REF:-}" || { echo "ADMIN_IMAGE_REF is required"; exit 1; }
  - apk add --no-cache openssh-client bash tar gzip
  - export TARGET_TAG="${DEPLOY_TAG:-${CI_COMMIT_SHORT_SHA}}"
  - eval "$(ssh-agent -s)"
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - printf '%s\n' "$DEPLOY_TARGET_SSH_KEY" | tr -d '\r' | ssh-add -
  - |
    if [ -n "${DEPLOY_TARGET_SSH_KNOWN_HOSTS:-}" ]; then
      printf '%s\n' "$DEPLOY_TARGET_SSH_KNOWN_HOSTS" >> ~/.ssh/known_hosts
    else
      echo "ERROR: DEPLOY_TARGET_SSH_KNOWN_HOSTS is not set. Set this CI variable to the deploy host's SSH public key." >&2
      echo "Generate with: ssh-keyscan -p PORT HOST" >&2
      exit 1
    fi
  - chmod 644 ~/.ssh/known_hosts
  - ./infra/ops/build-deploy-bundle.sh "${CI_PROJECT_DIR}/deploy-bundle.tgz"
  - ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "mkdir -p '${DEPLOY_TARGET_APP_DIR}'"
  - scp -P "${DEPLOY_TARGET_PORT:-22}" "${CI_PROJECT_DIR}/deploy-bundle.tgz" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}:${DEPLOY_TARGET_APP_DIR}/deploy-bundle.tgz"
  - |
    ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "
      set -euo pipefail &&
      cd '${DEPLOY_TARGET_APP_DIR}' &&
      tar xzf deploy-bundle.tgz &&
      rm -f deploy-bundle.tgz &&
      TAG='${TARGET_TAG}' BACKEND_IMAGE_REF='${BACKEND_IMAGE_REF}' FRONTEND_IMAGE_REF='${FRONTEND_IMAGE_REF}' ADMIN_IMAGE_REF='${ADMIN_IMAGE_REF}' \
      ./infra/ops/remote-preflight.sh &&
      TAG='${TARGET_TAG}' BACKEND_IMAGE_REF='${BACKEND_IMAGE_REF}' FRONTEND_IMAGE_REF='${FRONTEND_IMAGE_REF}' ADMIN_IMAGE_REF='${ADMIN_IMAGE_REF}' \
      ./infra/ops/remote-prod-deploy.sh
    "

.verify_remote_template:
  stage: verify
  image: alpine:3.21
  script:
  - test -n "${DEPLOY_TARGET_HOST:-}" || { echo "DEPLOY_TARGET_HOST is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_USER:-}" || { echo "DEPLOY_TARGET_USER is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_APP_DIR:-}" || { echo "DEPLOY_TARGET_APP_DIR is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_SSH_KEY:-}" || { echo "DEPLOY_TARGET_SSH_KEY is required"; exit 1; }
  - apk add --no-cache openssh-client bash curl
  - eval "$(ssh-agent -s)"
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - printf '%s\n' "$DEPLOY_TARGET_SSH_KEY" | tr -d '\r' | ssh-add -
  - |
    if [ -n "${DEPLOY_TARGET_SSH_KNOWN_HOSTS:-}" ]; then
      printf '%s\n' "$DEPLOY_TARGET_SSH_KNOWN_HOSTS" >> ~/.ssh/known_hosts
    else
      echo "ERROR: DEPLOY_TARGET_SSH_KNOWN_HOSTS is not set." >&2
      exit 1
    fi
  - chmod 644 ~/.ssh/known_hosts
  - |
    ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "
      set -euo pipefail &&
      cd '${DEPLOY_TARGET_APP_DIR}' &&
      ./infra/ops/smoke-check.sh &&
      ./infra/ops/observability-smoke-check.sh
    "

deploy_staging:
  extends: .deploy_remote_template
  needs:
  - package_backend
  - package_frontend
  - package_admin
  variables:
    DEPLOY_TARGET_HOST: $STAGING_DEPLOY_HOST
    DEPLOY_TARGET_PORT: $STAGING_DEPLOY_PORT
    DEPLOY_TARGET_USER: $STAGING_DEPLOY_USER
    DEPLOY_TARGET_APP_DIR: $STAGING_DEPLOY_APP_DIR
    DEPLOY_TARGET_SSH_KEY: $STAGING_DEPLOY_SSH_KEY
    DEPLOY_TARGET_SSH_KNOWN_HOSTS: $STAGING_DEPLOY_SSH_KNOWN_HOSTS
  environment:
    name: staging
    url: $STAGING_PUBLIC_URL
  rules:
  - if: '$CI_COMMIT_BRANCH == "develop" && $CI_PIPELINE_SOURCE == "push"'
  - when: never

deploy_production:
  extends: .deploy_remote_template
  needs:
  - package_backend
  - package_frontend
  - package_admin
  variables:
    DEPLOY_TARGET_HOST: $DEPLOY_HOST
    DEPLOY_TARGET_PORT: $DEPLOY_PORT
    DEPLOY_TARGET_USER: $DEPLOY_USER
    DEPLOY_TARGET_APP_DIR: $DEPLOY_APP_DIR
    DEPLOY_TARGET_SSH_KEY: $DEPLOY_SSH_KEY
    DEPLOY_TARGET_SSH_KNOWN_HOSTS: $DEPLOY_SSH_KNOWN_HOSTS
  environment:
    name: production
    url: $WEB_PUBLIC_URL
  rules:
  - if: '$CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"'
    when: manual
  - when: never

verify_staging:
  extends: .verify_remote_template
  needs: [deploy_staging]
  variables:
    DEPLOY_TARGET_HOST: $STAGING_DEPLOY_HOST
    DEPLOY_TARGET_PORT: $STAGING_DEPLOY_PORT
    DEPLOY_TARGET_USER: $STAGING_DEPLOY_USER
    DEPLOY_TARGET_APP_DIR: $STAGING_DEPLOY_APP_DIR
    DEPLOY_TARGET_SSH_KEY: $STAGING_DEPLOY_SSH_KEY
    DEPLOY_TARGET_SSH_KNOWN_HOSTS: $STAGING_DEPLOY_SSH_KNOWN_HOSTS
  environment:
    name: staging
    url: $STAGING_PUBLIC_URL
  rules:
  - if: '$CI_COMMIT_BRANCH == "develop" && $CI_PIPELINE_SOURCE == "push"'
  - when: never

verify_production:
  extends: .verify_remote_template
  needs: [deploy_production]
  variables:
    DEPLOY_TARGET_HOST: $DEPLOY_HOST
    DEPLOY_TARGET_PORT: $DEPLOY_PORT
    DEPLOY_TARGET_USER: $DEPLOY_USER
    DEPLOY_TARGET_APP_DIR: $DEPLOY_APP_DIR
    DEPLOY_TARGET_SSH_KEY: $DEPLOY_SSH_KEY
    DEPLOY_TARGET_SSH_KNOWN_HOSTS: $DEPLOY_SSH_KNOWN_HOSTS
  environment:
    name: production
    url: $WEB_PUBLIC_URL
  rules:
  - if: '$CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"'
  - when: never

rollback_staging:
  extends: .deploy_remote_template
  needs: []
  variables:
    DEPLOY_TARGET_HOST: $STAGING_DEPLOY_HOST
    DEPLOY_TARGET_PORT: $STAGING_DEPLOY_PORT
    DEPLOY_TARGET_USER: $STAGING_DEPLOY_USER
    DEPLOY_TARGET_APP_DIR: $STAGING_DEPLOY_APP_DIR
    DEPLOY_TARGET_SSH_KEY: $STAGING_DEPLOY_SSH_KEY
    DEPLOY_TARGET_SSH_KNOWN_HOSTS: $STAGING_DEPLOY_SSH_KNOWN_HOSTS
    DEPLOY_TAG: $ROLLBACK_TAG
  environment:
    name: staging
    url: $STAGING_PUBLIC_URL
  rules:
  - if: '$CI_COMMIT_BRANCH == "develop"'
    when: manual
  - when: never
  before_script: []
  script:
  - test -n "${DEPLOY_TARGET_HOST:-}" || { echo "DEPLOY_TARGET_HOST is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_USER:-}" || { echo "DEPLOY_TARGET_USER is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_APP_DIR:-}" || { echo "DEPLOY_TARGET_APP_DIR is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_SSH_KEY:-}" || { echo "DEPLOY_TARGET_SSH_KEY is required"; exit 1; }
  - apk add --no-cache openssh-client bash tar gzip
  - export TARGET_TAG="${DEPLOY_TAG:-}"
  - eval "$(ssh-agent -s)"
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - printf '%s\n' "$DEPLOY_TARGET_SSH_KEY" | tr -d '\r' | ssh-add -
  - |
    if [ -n "${DEPLOY_TARGET_SSH_KNOWN_HOSTS:-}" ]; then
      printf '%s\n' "$DEPLOY_TARGET_SSH_KNOWN_HOSTS" >> ~/.ssh/known_hosts
    else
      echo "ERROR: DEPLOY_TARGET_SSH_KNOWN_HOSTS is not set." >&2
      exit 1
    fi
  - chmod 644 ~/.ssh/known_hosts
  - ./infra/ops/build-deploy-bundle.sh "${CI_PROJECT_DIR}/deploy-bundle.tgz"
  - ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "mkdir -p '${DEPLOY_TARGET_APP_DIR}'"
  - scp -P "${DEPLOY_TARGET_PORT:-22}" "${CI_PROJECT_DIR}/deploy-bundle.tgz" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}:${DEPLOY_TARGET_APP_DIR}/deploy-bundle.tgz"
  - |
    ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "
      set -euo pipefail &&
      cd '${DEPLOY_TARGET_APP_DIR}' &&
      tar xzf deploy-bundle.tgz &&
      rm -f deploy-bundle.tgz &&
      TAG='${TARGET_TAG}' ROLLBACK_TAG='${TARGET_TAG}' ./infra/ops/remote-prod-rollback.sh
    "

rollback_production:
  extends: .deploy_remote_template
  needs: []
  variables:
    DEPLOY_TARGET_HOST: $DEPLOY_HOST
    DEPLOY_TARGET_PORT: $DEPLOY_PORT
    DEPLOY_TARGET_USER: $DEPLOY_USER
    DEPLOY_TARGET_APP_DIR: $DEPLOY_APP_DIR
    DEPLOY_TARGET_SSH_KEY: $DEPLOY_SSH_KEY
    DEPLOY_TARGET_SSH_KNOWN_HOSTS: $DEPLOY_SSH_KNOWN_HOSTS
    DEPLOY_TAG: $ROLLBACK_TAG
  environment:
    name: production
    url: $WEB_PUBLIC_URL
  rules:
  - if: '$CI_COMMIT_BRANCH == "main"'
    when: manual
  - when: never
  before_script: []
  script:
  - test -n "${DEPLOY_TARGET_HOST:-}" || { echo "DEPLOY_TARGET_HOST is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_USER:-}" || { echo "DEPLOY_TARGET_USER is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_APP_DIR:-}" || { echo "DEPLOY_TARGET_APP_DIR is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_SSH_KEY:-}" || { echo "DEPLOY_TARGET_SSH_KEY is required"; exit 1; }
  - apk add --no-cache openssh-client bash tar gzip
  - export TARGET_TAG="${DEPLOY_TAG:-}"
  - eval "$(ssh-agent -s)"
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - printf '%s\n' "$DEPLOY_TARGET_SSH_KEY" | tr -d '\r' | ssh-add -
  - |
    if [ -n "${DEPLOY_TARGET_SSH_KNOWN_HOSTS:-}" ]; then
      printf '%s\n' "$DEPLOY_TARGET_SSH_KNOWN_HOSTS" >> ~/.ssh/known_hosts
    else
      echo "ERROR: DEPLOY_TARGET_SSH_KNOWN_HOSTS is not set." >&2
      exit 1
    fi
  - chmod 644 ~/.ssh/known_hosts
  - ./infra/ops/build-deploy-bundle.sh "${CI_PROJECT_DIR}/deploy-bundle.tgz"
  - ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "mkdir -p '${DEPLOY_TARGET_APP_DIR}'"
  - scp -P "${DEPLOY_TARGET_PORT:-22}" "${CI_PROJECT_DIR}/deploy-bundle.tgz" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}:${DEPLOY_TARGET_APP_DIR}/deploy-bundle.tgz"
  - |
    ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "
      set -euo pipefail &&
      cd '${DEPLOY_TARGET_APP_DIR}' &&
      tar xzf deploy-bundle.tgz &&
      rm -f deploy-bundle.tgz &&
      TAG='${TARGET_TAG}' ROLLBACK_TAG='${TARGET_TAG}' ./infra/ops/remote-prod-rollback.sh
    "
