.package_image_template:
  stage: package
  extends: .docker_job
  rules:
  - if: '$CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"'
  - if: '$CI_COMMIT_BRANCH == "develop" && $CI_PIPELINE_SOURCE == "push"'
  - when: never

.deploy_remote_template:
  stage: deploy
  image: alpine:3.21
  script:
  - test -n "${DEPLOY_TARGET_HOST:-}" || { echo "DEPLOY_TARGET_HOST is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_USER:-}" || { echo "DEPLOY_TARGET_USER is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_APP_DIR:-}" || { echo "DEPLOY_TARGET_APP_DIR is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_SSH_KEY:-}" || { echo "DEPLOY_TARGET_SSH_KEY is required"; exit 1; }
  - test -n "${BACKEND_IMAGE_REF:-}" || { echo "BACKEND_IMAGE_REF is required"; exit 1; }
  - test -n "${FRONTEND_IMAGE_REF:-}" || { echo "FRONTEND_IMAGE_REF is required"; exit 1; }
  - test -n "${ADMIN_IMAGE_REF:-}" || { echo "ADMIN_IMAGE_REF is required"; exit 1; }
  - apk add --no-cache openssh-client bash tar gzip
  - export TARGET_TAG="${DEPLOY_TAG:-${CI_COMMIT_SHORT_SHA}}"
  - eval "$(ssh-agent -s)"
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - printf '%s\n' "$DEPLOY_TARGET_SSH_KEY" | tr -d '\r' | ssh-add -
  - ssh-keyscan -p "${DEPLOY_TARGET_PORT:-22}" "$DEPLOY_TARGET_HOST" >> ~/.ssh/known_hosts
  - ./infra/ops/build-deploy-bundle.sh "${CI_PROJECT_DIR}/deploy-bundle.tgz"
  - ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "mkdir -p '${DEPLOY_TARGET_APP_DIR}'"
  - scp -P "${DEPLOY_TARGET_PORT:-22}" "${CI_PROJECT_DIR}/deploy-bundle.tgz" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}:${DEPLOY_TARGET_APP_DIR}/deploy-bundle.tgz"
  - |
    ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "
      set -euo pipefail &&
      cd '${DEPLOY_TARGET_APP_DIR}' &&
      tar xzf deploy-bundle.tgz &&
      rm -f deploy-bundle.tgz &&
      TAG='${TARGET_TAG}' BACKEND_IMAGE_REF='${BACKEND_IMAGE_REF}' FRONTEND_IMAGE_REF='${FRONTEND_IMAGE_REF}' ADMIN_IMAGE_REF='${ADMIN_IMAGE_REF}' \
      ./infra/ops/remote-preflight.sh &&
      TAG='${TARGET_TAG}' BACKEND_IMAGE_REF='${BACKEND_IMAGE_REF}' FRONTEND_IMAGE_REF='${FRONTEND_IMAGE_REF}' ADMIN_IMAGE_REF='${ADMIN_IMAGE_REF}' \
      ./infra/ops/remote-prod-deploy.sh
    "

.verify_remote_template:
  stage: verify
  image: alpine:3.21
  script:
  - test -n "${DEPLOY_TARGET_HOST:-}" || { echo "DEPLOY_TARGET_HOST is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_USER:-}" || { echo "DEPLOY_TARGET_USER is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_APP_DIR:-}" || { echo "DEPLOY_TARGET_APP_DIR is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_SSH_KEY:-}" || { echo "DEPLOY_TARGET_SSH_KEY is required"; exit 1; }
  - apk add --no-cache openssh-client bash curl
  - eval "$(ssh-agent -s)"
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - printf '%s\n' "$DEPLOY_TARGET_SSH_KEY" | tr -d '\r' | ssh-add -
  - ssh-keyscan -p "${DEPLOY_TARGET_PORT:-22}" "$DEPLOY_TARGET_HOST" >> ~/.ssh/known_hosts
  - |
    ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "
      set -euo pipefail &&
      cd '${DEPLOY_TARGET_APP_DIR}' &&
      ./infra/ops/smoke-check.sh &&
      ./infra/ops/observability-smoke-check.sh
    "

package_backend:
  extends: .package_image_template
  script:
  - echo "$REGISTRY_PASSWORD" | docker login "$REGISTRY" --username "$REGISTRY_USERNAME" --password-stdin
  - docker pull "${BACKEND_IMAGE}:ci-${CI_COMMIT_SHORT_SHA}"
  - docker tag "${BACKEND_IMAGE}:ci-${CI_COMMIT_SHORT_SHA}" "${BACKEND_IMAGE}:${CI_COMMIT_SHORT_SHA}"
  - |-
    if [ "${CI_COMMIT_BRANCH}" = "main" ]; then
      docker tag "${BACKEND_IMAGE}:ci-${CI_COMMIT_SHORT_SHA}" "${BACKEND_IMAGE}:latest"
    else
      docker tag "${BACKEND_IMAGE}:ci-${CI_COMMIT_SHORT_SHA}" "${BACKEND_IMAGE}:develop-latest"
    fi
  - docker push "${BACKEND_IMAGE}:${CI_COMMIT_SHORT_SHA}"
  - printf 'BACKEND_IMAGE_REF=%s\n' "${BACKEND_IMAGE}:${CI_COMMIT_SHORT_SHA}" > backend-image.env
  - |-
    if [ "${CI_COMMIT_BRANCH}" = "main" ]; then
      docker push "${BACKEND_IMAGE}:latest"
    else
      docker push "${BACKEND_IMAGE}:develop-latest"
    fi
  artifacts:
    reports:
      dotenv: backend-image.env
  needs:
  - openapi_contract
  - backend_lint
  - backend_security
  - backend_test
  - migration_verify
  - backend_vulnerability_scan
  - backend_build
  - docker_build_backend
  - container_scan_backend
package_frontend:
  extends: .package_image_template
  script:
  - echo "$REGISTRY_PASSWORD" | docker login "$REGISTRY" --username "$REGISTRY_USERNAME" --password-stdin
  - docker pull "${FRONTEND_IMAGE}:ci-${CI_COMMIT_SHORT_SHA}"
  - docker tag "${FRONTEND_IMAGE}:ci-${CI_COMMIT_SHORT_SHA}" "${FRONTEND_IMAGE}:${CI_COMMIT_SHORT_SHA}"
  - |-
    if [ "${CI_COMMIT_BRANCH}" = "main" ]; then
      docker tag "${FRONTEND_IMAGE}:ci-${CI_COMMIT_SHORT_SHA}" "${FRONTEND_IMAGE}:latest"
    else
      docker tag "${FRONTEND_IMAGE}:ci-${CI_COMMIT_SHORT_SHA}" "${FRONTEND_IMAGE}:develop-latest"
    fi
  - docker push "${FRONTEND_IMAGE}:${CI_COMMIT_SHORT_SHA}"
  - printf 'FRONTEND_IMAGE_REF=%s\n' "${FRONTEND_IMAGE}:${CI_COMMIT_SHORT_SHA}" > frontend-image.env
  - |-
    if [ "${CI_COMMIT_BRANCH}" = "main" ]; then
      docker push "${FRONTEND_IMAGE}:latest"
    else
      docker push "${FRONTEND_IMAGE}:develop-latest"
    fi
  artifacts:
    reports:
      dotenv: frontend-image.env
  needs:
  - openapi_contract
  - frontend_lint
  - frontend_typecheck
  - frontend_test
  - frontend_e2e
  - frontend_dependency_scan
  - frontend_build
  - docker_build_frontend
  - container_scan_frontend
package_admin:
  extends: .package_image_template
  script:
  - echo "$REGISTRY_PASSWORD" | docker login "$REGISTRY" --username "$REGISTRY_USERNAME" --password-stdin
  - docker pull "${ADMIN_IMAGE}:ci-${CI_COMMIT_SHORT_SHA}"
  - docker tag "${ADMIN_IMAGE}:ci-${CI_COMMIT_SHORT_SHA}" "${ADMIN_IMAGE}:${CI_COMMIT_SHORT_SHA}"
  - |-
    if [ "${CI_COMMIT_BRANCH}" = "main" ]; then
      docker tag "${ADMIN_IMAGE}:ci-${CI_COMMIT_SHORT_SHA}" "${ADMIN_IMAGE}:latest"
    else
      docker tag "${ADMIN_IMAGE}:ci-${CI_COMMIT_SHORT_SHA}" "${ADMIN_IMAGE}:develop-latest"
    fi
  - docker push "${ADMIN_IMAGE}:${CI_COMMIT_SHORT_SHA}"
  - printf 'ADMIN_IMAGE_REF=%s\n' "${ADMIN_IMAGE}:${CI_COMMIT_SHORT_SHA}" > admin-image.env
  - |-
    if [ "${CI_COMMIT_BRANCH}" = "main" ]; then
      docker push "${ADMIN_IMAGE}:latest"
    else
      docker push "${ADMIN_IMAGE}:develop-latest"
    fi
  artifacts:
    reports:
      dotenv: admin-image.env
  needs:
  - openapi_contract
  - admin_lint
  - admin_typecheck
  - admin_e2e
  - admin_dependency_scan
  - admin_build
  - docker_build_admin
  - container_scan_admin
deploy_staging:
  extends: .deploy_remote_template
  needs:
  - package_backend
  - package_frontend
  - package_admin
  variables:
    DEPLOY_TARGET_HOST: $STAGING_DEPLOY_HOST
    DEPLOY_TARGET_PORT: $STAGING_DEPLOY_PORT
    DEPLOY_TARGET_USER: $STAGING_DEPLOY_USER
    DEPLOY_TARGET_APP_DIR: $STAGING_DEPLOY_APP_DIR
    DEPLOY_TARGET_SSH_KEY: $STAGING_DEPLOY_SSH_KEY
  environment:
    name: staging
    url: $STAGING_PUBLIC_URL
  rules:
  - if: '$CI_COMMIT_BRANCH == "develop" && $CI_PIPELINE_SOURCE == "push"'
  - when: never

deploy_production:
  extends: .deploy_remote_template
  needs:
  - package_backend
  - package_frontend
  - package_admin
  variables:
    DEPLOY_TARGET_HOST: $DEPLOY_HOST
    DEPLOY_TARGET_PORT: $DEPLOY_PORT
    DEPLOY_TARGET_USER: $DEPLOY_USER
    DEPLOY_TARGET_APP_DIR: $DEPLOY_APP_DIR
    DEPLOY_TARGET_SSH_KEY: $DEPLOY_SSH_KEY
  environment:
    name: production
    url: $WEB_PUBLIC_URL
  rules:
  - if: '$CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"'
    when: manual
  - when: never

verify_staging:
  extends: .verify_remote_template
  needs: [deploy_staging]
  variables:
    DEPLOY_TARGET_HOST: $STAGING_DEPLOY_HOST
    DEPLOY_TARGET_PORT: $STAGING_DEPLOY_PORT
    DEPLOY_TARGET_USER: $STAGING_DEPLOY_USER
    DEPLOY_TARGET_APP_DIR: $STAGING_DEPLOY_APP_DIR
    DEPLOY_TARGET_SSH_KEY: $STAGING_DEPLOY_SSH_KEY
  environment:
    name: staging
    url: $STAGING_PUBLIC_URL
  rules:
  - if: '$CI_COMMIT_BRANCH == "develop" && $CI_PIPELINE_SOURCE == "push"'
  - when: never

verify_production:
  extends: .verify_remote_template
  needs: [deploy_production]
  variables:
    DEPLOY_TARGET_HOST: $DEPLOY_HOST
    DEPLOY_TARGET_PORT: $DEPLOY_PORT
    DEPLOY_TARGET_USER: $DEPLOY_USER
    DEPLOY_TARGET_APP_DIR: $DEPLOY_APP_DIR
    DEPLOY_TARGET_SSH_KEY: $DEPLOY_SSH_KEY
  environment:
    name: production
    url: $WEB_PUBLIC_URL
  rules:
  - if: '$CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"'
  - when: never

rollback_staging:
  extends: .deploy_remote_template
  needs: []
  variables:
    DEPLOY_TARGET_HOST: $STAGING_DEPLOY_HOST
    DEPLOY_TARGET_PORT: $STAGING_DEPLOY_PORT
    DEPLOY_TARGET_USER: $STAGING_DEPLOY_USER
    DEPLOY_TARGET_APP_DIR: $STAGING_DEPLOY_APP_DIR
    DEPLOY_TARGET_SSH_KEY: $STAGING_DEPLOY_SSH_KEY
    DEPLOY_TAG: $ROLLBACK_TAG
  environment:
    name: staging
    url: $STAGING_PUBLIC_URL
  rules:
  - if: '$CI_COMMIT_BRANCH == "develop"'
    when: manual
  - when: never
  before_script: []
  script:
  - test -n "${DEPLOY_TARGET_HOST:-}" || { echo "DEPLOY_TARGET_HOST is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_USER:-}" || { echo "DEPLOY_TARGET_USER is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_APP_DIR:-}" || { echo "DEPLOY_TARGET_APP_DIR is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_SSH_KEY:-}" || { echo "DEPLOY_TARGET_SSH_KEY is required"; exit 1; }
  - apk add --no-cache openssh-client bash tar gzip
  - export TARGET_TAG="${DEPLOY_TAG:-}"
  - eval "$(ssh-agent -s)"
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - printf '%s\n' "$DEPLOY_TARGET_SSH_KEY" | tr -d '\r' | ssh-add -
  - ssh-keyscan -p "${DEPLOY_TARGET_PORT:-22}" "$DEPLOY_TARGET_HOST" >> ~/.ssh/known_hosts
  - ./infra/ops/build-deploy-bundle.sh "${CI_PROJECT_DIR}/deploy-bundle.tgz"
  - ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "mkdir -p '${DEPLOY_TARGET_APP_DIR}'"
  - scp -P "${DEPLOY_TARGET_PORT:-22}" "${CI_PROJECT_DIR}/deploy-bundle.tgz" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}:${DEPLOY_TARGET_APP_DIR}/deploy-bundle.tgz"
  - |
    ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "
      set -euo pipefail &&
      cd '${DEPLOY_TARGET_APP_DIR}' &&
      tar xzf deploy-bundle.tgz &&
      rm -f deploy-bundle.tgz &&
      TAG='${TARGET_TAG}' ROLLBACK_TAG='${TARGET_TAG}' ./infra/ops/remote-prod-rollback.sh
    "

rollback_production:
  extends: .deploy_remote_template
  needs: []
  variables:
    DEPLOY_TARGET_HOST: $DEPLOY_HOST
    DEPLOY_TARGET_PORT: $DEPLOY_PORT
    DEPLOY_TARGET_USER: $DEPLOY_USER
    DEPLOY_TARGET_APP_DIR: $DEPLOY_APP_DIR
    DEPLOY_TARGET_SSH_KEY: $DEPLOY_SSH_KEY
    DEPLOY_TAG: $ROLLBACK_TAG
  environment:
    name: production
    url: $WEB_PUBLIC_URL
  rules:
  - if: '$CI_COMMIT_BRANCH == "main"'
    when: manual
  - when: never
  before_script: []
  script:
  - test -n "${DEPLOY_TARGET_HOST:-}" || { echo "DEPLOY_TARGET_HOST is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_USER:-}" || { echo "DEPLOY_TARGET_USER is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_APP_DIR:-}" || { echo "DEPLOY_TARGET_APP_DIR is required"; exit 1; }
  - test -n "${DEPLOY_TARGET_SSH_KEY:-}" || { echo "DEPLOY_TARGET_SSH_KEY is required"; exit 1; }
  - apk add --no-cache openssh-client bash tar gzip
  - export TARGET_TAG="${DEPLOY_TAG:-}"
  - eval "$(ssh-agent -s)"
  - mkdir -p ~/.ssh
  - chmod 700 ~/.ssh
  - printf '%s\n' "$DEPLOY_TARGET_SSH_KEY" | tr -d '\r' | ssh-add -
  - ssh-keyscan -p "${DEPLOY_TARGET_PORT:-22}" "$DEPLOY_TARGET_HOST" >> ~/.ssh/known_hosts
  - ./infra/ops/build-deploy-bundle.sh "${CI_PROJECT_DIR}/deploy-bundle.tgz"
  - ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "mkdir -p '${DEPLOY_TARGET_APP_DIR}'"
  - scp -P "${DEPLOY_TARGET_PORT:-22}" "${CI_PROJECT_DIR}/deploy-bundle.tgz" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}:${DEPLOY_TARGET_APP_DIR}/deploy-bundle.tgz"
  - |
    ssh -p "${DEPLOY_TARGET_PORT:-22}" "${DEPLOY_TARGET_USER}@${DEPLOY_TARGET_HOST}" "
      set -euo pipefail &&
      cd '${DEPLOY_TARGET_APP_DIR}' &&
      tar xzf deploy-bundle.tgz &&
      rm -f deploy-bundle.tgz &&
      TAG='${TARGET_TAG}' ROLLBACK_TAG='${TARGET_TAG}' ./infra/ops/remote-prod-rollback.sh
    "
