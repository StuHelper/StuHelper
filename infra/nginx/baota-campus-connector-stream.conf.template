# Managed by StuHelper. This file is included inside Baota's top-level
# `stream {}` block. TLS must pass through unchanged to the StuHelper Gateway.
server {
    listen __PUBLIC_PORT__;
    proxy_connect_timeout 5s;
    proxy_timeout 15m;
    proxy_pass 127.0.0.1:__UPSTREAM_PORT__;

__ALLOW_DIRECTIVES__
    deny all;
}
